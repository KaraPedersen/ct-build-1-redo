// Write a function `numberify` that takes a string and converts it into a number:
// ```js
// const n = numberify('100');
// // n is 100
// ```
// * Red - write a test
// * Green - implement `numberify` using `Number(...)` to convert the string to a number
// * Refactor - use the `+` operator to convert the string to a number (`+someString` converts `someString` into a number)
